# 跨设备剪贴板：产品与架构设计

本文记录当前讨论形成的产品约束和模块边界。明确区分已确认事项、建议默认行为和待定实现细节，供后续开发与评审使用。

## 1. 已确认的产品范围

| 项目 | 约定 |
| --- | --- |
| 目标平台 | macOS 与 Windows |
| 网络范围 | 同一局域网，或已有 VPN 中可直接连通的设备 |
| 开发语言 | Rust 承担核心逻辑 |
| 桌面框架 | Tauri 2 |
| Web 前端 | React + TypeScript；采用用户熟悉的技术栈 |
| macOS 剪贴板监听 | 接受轮询策略 |
| 剪贴板实现 | 自研 clipboard crate 的实现不依赖 arboard，分别封装平台原生 API；项目其他依赖间接引入 arboard 不受此约束限制 |
| 普通文字 | 支持自动同步和手动发送 |
| 图片、文件 | 仅支持手动发送 |
| 历史记录 | 对文字且仅对文字记录历史 |
| 图片、文件接收 | 先保存到接收端用户设定的默认保存目录，成功后覆盖接收端剪贴板 |
| 完成时覆盖策略 | 即使接收端在传输期间复制了其他内容，传输完成后仍然覆盖剪贴板 |
| 代码组织 | 模块职责单一、边界清晰、依赖方向固定，核心业务独立于 Tauri |
| 业务库依赖方向 | Tauri 应用依赖 core，由 core 依赖并组装 clipboard、network、storage |
| 第一轮范围 | 先开发 core、clipboard、network、storage 四个 Rust 库，通过测试和最小示例验证；前端与桌面程序留到后续轮次 |

## 2. 建议采用的交互与数据规则

本节是讨论中提出的工作草案；尚未由用户单独确定的默认值和细节，实施前可以继续调整。

### 2.1 文字同步

- 自动模式在本机产生新的文字复制后，发送给已启用同步的可信设备。
- 手动模式在用户触发发送操作时读取并发送当前内容。
- 两种模式控制发送时机；接收权限独立设置。
- 提供暂停收发的入口。历史记录是否跟随暂停，仍需明确。
- 接收到文字后写入系统剪贴板，由用户执行粘贴。
- 断线重连后默认不补发旧剪贴板，从后续新复制继续同步；用户可主动重新发送。
- 远端接收引起的本机剪贴板变化不能再次作为新内容发送。

### 2.2 图片与文件

建议保留用户原始复制动作的语义：

| 来源 | 接收端保存 | 写入剪贴板 |
| --- | --- | --- |
| 截图、应用中的“复制图片” | 图片文件，初期可统一为 PNG | 图片内容 |
| Finder 或资源管理器中复制的文件，包括图片文件 | 原始文件内容与适配后的文件名 | 接收端本地文件引用 |

接收流程：

1. 将数据写入默认保存目录内的临时文件。
2. 完成传输并校验完整性。
3. 将临时文件提交为正式文件。
4. 使用正式文件或图片内容覆盖剪贴板。
5. 向接收端提示结果，并向发送端反馈状态。

建议的边界规则：

- 同名文件自动生成不冲突的新名称，不覆盖已有文件。
- 一批多个文件全部成功保存后，再一次性写入文件引用列表。
- 失败、取消及未完成的传输不更新剪贴板。
- 文件已保存但剪贴板更新失败时，保留正式文件，分别报告保存结果与剪贴板结果，并允许重试复制。
- 接收目录中的正式文件属于用户保存的内容，不按临时缓存自动清理。
- 图片、文件及其附带的路径、文件名或替代文本，不进入文字历史。
- 网络提供的文件名只用于在所选目录内创建文件，不直接作为任意本地写入路径。

文件夹、虚拟文件、文件变化期间的发送行为、大小上限、并发任务顺序和断点续传尚待细化。

### 2.3 文字历史

- 建议记录本机复制和远端接收的文字，并保留来源信息。
- 先识别剪贴板内容类型，再决定是否入库，避免把文件或图片的附带表示当成文字。
- 用户明确复制的路径字符串仍属于普通文字。
- 建议提供搜索、去重、删除和保留上限；具体规则待定。
- 历史默认保存在本机，配对时不批量共享既有记录；是否需要完整历史跨设备同步尚未确定。
- 手动发送模式下，只要历史记录开启，仍需持续监控剪贴板。

## 3. 已确认的模块边界

使用 Cargo workspace，第一轮先开发 core、clipboard、network、storage 四个 Rust 库，各库按职责独立组织和测试，通过 core 的最小命令行示例验证完整流程。Tauri 桌面应用在后续轮次接入，本轮不创建前端或桌面应用工程。

`core` 是独立于界面的完整应用后端，负责底层模块的组装、生命周期与业务协调。依赖组装发生在 `core`，最终链接由调用它的可执行程序完成：第一轮是最小示例程序，后续是桌面应用；四个库不是四个需要分别启动的后台服务。

这里区分代码边界与进程边界：第一轮的 Rust 业务模块运行在示例程序的进程中。后续接入 Tauri 时，它们运行在 Tauri 主进程中，前端由系统 WebView 的相关进程承载，因此整个桌面应用并非只有一个操作系统进程。Rust 后端内部可以使用异步任务和工作线程，不意味着所有工作都在同一条线程上串行执行。

后续如果需要让同步服务在整个桌面应用退出后仍独立运行，再另行设计常驻服务、生命周期和进程间通信；当前的模块化约定不自动引入这些部署要求。

完整产品的目标目录如下；第一轮只建立四个库及 core 的验证示例，`apps/desktop` 留到后续轮次。

```text
crates/
├── core/                 # 完整应用后端：业务规则、模块组装与流程协调
│   └── examples/         # 第一轮的最小调用与双设备联调示例
├── clipboard/            # macOS、Windows 剪贴板适配
├── network/              # 设备发现、连接与传输协议
└── storage/              # 历史数据库、配置与接收文件存储

apps/
└── desktop/
    ├── src/              # React + TypeScript 前端界面
    └── src-tauri/        # 启动 core、桌面交互、命令与事件桥接
```

### 3.1 core

| 模块 | 职责 |
| --- | --- |
| lib.rs | 声明模块并导出后端公共接口 |
| app | 面向桌面应用等入口的操作接口、状态查询与事件订阅 |
| bootstrap | 创建和连接底层模块，管理后端启动与关闭 |
| devices | 配对流程、信任关系、设备管理 |
| sync | 文字同步策略、重复消息与回环处理 |
| transfer | 手动传输任务、进度、取消、完成后的流程协调 |
| history | 文字入库规则、去重、保留策略、搜索和删除用例 |
| model | 应用层内容、设备、任务、配置和事件的数据类型 |

`core` 依赖并调用 `clipboard`、`network` 和 `storage` 的公共接口，决定何时执行操作及失败后的业务行为。系统剪贴板 API、网络协议实现和 SQLite 访问仍分别封装在底层库中；`core` 不依赖 Tauri，也不接收 Tauri 专用的应用或窗口对象。文字手动发送复用文字同步能力，图片与文件由传输用例协调。

对外提供发送当前剪贴板、查询历史、暂停同步、管理设备、查询状态及订阅事件等能力。Tauri 通过这些入口使用后端，无需直接持有底层连接或数据库句柄。

业务规则与 `bootstrap` 中的具体实例创建分开。需要替换外部能力进行测试时，可以在 `core` 内定义小型接口，并在内部适配包装中调用底层库；底层库无需实现或依赖 `core` 的接口。不要预先为所有内部调用增加抽象层。

### 3.2 clipboard

负责系统剪贴板的类型识别、读取、写入和变化采集。macOS 与 Windows 的实现分别组织，由本库定义统一的公共接口和剪贴板数据类型，供 `core` 调用。

- 自研 `clipboard` crate 的实现不依赖 arboard。这个约束只针对该库的实现，不要求整个应用的依赖树排除 arboard；其他插件或第三方库间接引入 arboard 是允许的。
- Tauri 官方 `clipboard-manager` 提供剪贴板读写接口，其桌面实现依赖 arboard。它属于桌面层可按需选择的插件，并非禁用项；自研 `clipboard` 库的原生实现不委托给该插件。
- macOS：直接封装 AppKit 的 `NSPasteboard`，轮询 `changeCount`，变化后再读取必要内容；建议通过 objc2 及 AppKit、Foundation 的 Rust 绑定调用原生 API。
- Windows：直接封装 Win32 剪贴板 API，以 `AddClipboardFormatListener` / `WM_CLIPBOARDUPDATE` 采集变化；使用 windows-rs 提供或生成的必要 API 绑定，具体绑定方案实施时确定。
- 系统 API 绑定仅用于访问平台能力，跨平台剪贴板语义由本库维护。平台依赖按目标系统启用，系统对象和 unsafe 实现细节封装在平台模块内部。
- 图片内容与文件引用的系统格式转换集中在这里。
- 自行处理文本编码、图片格式和透明通道、文件引用、剪贴板占用重试，以及系统对象的内存所有权和线程约束，并在两种系统上验证。
- 提供识别应用自身写入所需的信息，由核心配合消息身份处理回环。
- 不决定发送对象、不操作历史数据库、不管理网络连接。

### 3.3 network

负责设备发现、地址连接、加密传输、消息编解码和网络层状态。本库定义连接、协议及网络事件类型；`core` 将业务层信任关系转换为连接所需的可信身份信息，由网络层执行连接身份校验。

内部按发现、连接、协议等职责拆分。建议提供局域网自动发现与手动地址添加，两者共用连接协议；不能假定 VPN 会转发局域网发现所需的组播。

网络模块报告连接和传输结果，不直接写剪贴板或历史数据库。

### 3.4 storage

内部按历史存储、配置存储、接收文件存储分别组织。

本库定义持久化操作所需的记录、查询参数和结果类型，由 `core` 在应用模型与存储模型之间转换；存储层不反向导入 `core` 的类型。

- 历史存储执行查询和读写，历史业务规则由 `core/history` 决定。
- 配置存储负责设置持久化。
- 接收文件存储负责临时文件、流式写入、完整性校验、重名处理及正式文件提交。
- 不主动发起同步，不负责完成后覆盖剪贴板的业务决策。

设备私钥的系统安全存储适配方式另行确定。

### 3.5 desktop（后续轮次）

Tauri 应用通过 `core` 的公共入口启动和关闭后端、调用业务操作并订阅事件，同时负责命令注册、状态转发，以及托盘、窗口和快捷键等桌面交互。剪贴板、网络和存储实例统一由 `core` 组装。

- Web 前端使用 React + TypeScript，构建工具建议沿用 Vite；组件库尚未选定。
- 命令函数只处理参数、调用业务入口并返回结果。
- 在项目业务库范围内只直接依赖 `core`；桌面框架、托盘等 UI 集成依赖仍由 Tauri 应用管理。
- 剪贴板同步、历史恢复和传输完成后的写入通过 `core` 调用自研 `clipboard`，保持业务规则和写入来源处理一致；桌面插件按实际功能需求选用，不因其间接依赖 arboard 而排除。
- 前端展示状态并提交操作请求；连接和传输的权威状态保留在 Rust 侧。
- 图片编码、文件读写及传输放在 Rust 侧，界面接收进度、结果与必要预览。
- 前端组件按设备、历史、传输和设置等功能组织，避免把所有界面与状态放进单个组件。

### 3.6 依赖约束

```text
desktop/src-tauri → core → clipboard
                        → network
                        → storage
```

- `core` 依赖底层库，底层库不依赖 `core`，避免循环依赖。
- `clipboard`、`network`、`storage` 之间不直接互相依赖，由 `core` 协调业务流程。
- 底层库各自定义公共接口所需的类型，由 `core` 负责应用层转换。只有出现确实需要共享的稳定类型时，才考虑增加小型公共类型库；公共类型库不能依赖上述业务库。
- `core` 和底层库均不依赖 Tauri，允许其他程序入口复用完整后端。
- 每类状态有明确所有者，避免在多个模块维护独立副本。
- 仅在外部能力边界引入接口；内部函数不为抽象而抽象。
- 辅助逻辑放在对应业务附近，不建立不断扩张的通用 utils 或 manager。
- 从第一版开始落实上述职责边界。后续扩展若让某个模块混入另一种独立职责，就拆为职责明确的子模块，必要时再拆为独立 crate；这种代码拆分不代表拆成独立进程。

## 4. 候选技术与待定细节

以下技术在讨论中获得推荐，但尚未逐项锁定：

| 能力 | 候选方案 |
| --- | --- |
| 前端构建 | Vite；界面框架已确定为 React + TypeScript，组件库另行选择 |
| 异步运行时 | Tokio |
| 连接传输 | TCP + TLS，使用 rustls |
| 局域网发现 | mdns-sd，辅以手动 IP 或主机名及端口 |
| 原生剪贴板 API 绑定 | 自研 clipboard crate 使用 macOS 的 objc2 与 AppKit、Foundation 绑定，以及 Windows 的必要 Win32 绑定；该库的实现不用 arboard |
| 文字历史 | SQLite |
| macOS 轮询间隔 | 先以 200–300 ms 为测量起点 |

仍需确定：

1. 前端组件库、支持的系统最低版本和分发方式。
2. 配对交互、密钥存储、身份验证及解除信任的流程。
3. 两端同时复制时的冲突规则，以及多个传输完成时的剪贴板覆盖顺序。
4. 文字历史的保留上限、去重规则、敏感内容处理和暂停行为。
5. 图片、文件的范围与大小限制、取消和失败恢复行为。

## 5. 建议的实现与验证顺序

第一轮开发范围、建议默认行为与验收标准详见 [第一轮开发目标](MILESTONE-01.md)。第一轮聚焦四个 Rust 库，通过测试和最小命令行示例验证两台设备的文字同步与本地历史，使用手动地址配对。Tauri / React 桌面程序、托盘、快捷键，以及图片、文件和自动发现安排在后续轮次。首轮新增默认值仍是实施建议，不替代本文已经确认的产品约束。

1. 按已确认方向建立 workspace 依赖，定义底层公共接口及 core 应用模型，在 core 内组装后端；将业务规则与实例创建分开，使用必要的内存替身验证规则。
2. 打通两端纯文字的采集、连接、自动同步、手动发送及回环处理。
3. 完成文字历史与设备管理接口，通过 core 最小示例和双端联调完成第一轮验收。
4. 后续接入 Tauri + React + TypeScript，加入托盘、快捷键和设备管理、历史界面。
5. 加入图片手动发送、保存和剪贴板写入。
6. 加入文件手动传输、批量保存、失败恢复和剪贴板写入。

关键验证应覆盖远端写入不回传、仅文字入库、重连不重放旧内容，以及传输成功后覆盖、失败时不覆盖的规则。另在两种操作系统上验证原生图片与文件粘贴、睡眠唤醒和网络切换。

macOS 轮询可能遗漏两次检查之间已经被替换的中间内容，因此历史不能承诺捕获每一次快速连续复制。检查间隔与空闲资源占用需要实测。

## 6. 已查阅的技术依据

- [Apple：NSPasteboard.changeCount](https://developer.apple.com/documentation/appkit/nspasteboard/changecount)
- [Microsoft：AddClipboardFormatListener](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-addclipboardformatlistener)
- [Microsoft：文件剪贴板格式](https://learn.microsoft.com/en-us/windows/win32/shell/clipboard)
- [Apple：剪贴板文件 URL 类型](https://developer.apple.com/documentation/appkit/nspasteboard/pasteboardtype/fileurl)
- [objc2-app-kit：NSPasteboard 绑定](https://docs.rs/objc2-app-kit/latest/objc2_app_kit/struct.NSPasteboard.html)
- [Microsoft：windows-rs](https://github.com/microsoft/windows-rs)
- [Tauri：clipboard-manager 功能](https://v2.tauri.app/plugin/clipboard/)
- [Tauri：clipboard-manager 依赖清单](https://github.com/tauri-apps/plugins-workspace/blob/v2/plugins/clipboard-manager/Cargo.toml)
- [Tauri：系统托盘](https://v2.tauri.app/learn/system-tray/)
- [Tauri：进程模型](https://v2.tauri.app/concept/process-model/)
- [Tauri：全局快捷键](https://v2.tauri.app/plugin/global-shortcut/)
- [RFC 6762：Multicast DNS](https://www.rfc-editor.org/rfc/rfc6762)
