# 第一轮开发目标：四个 Rust 库与双设备文字流程

状态：用户已确定第一轮聚焦 core、clipboard、network、storage 四个 Rust 库，暂不开发前端或桌面程序；尚未开始功能实现。已确认的产品与架构约束以 [DESIGN.md](DESIGN.md) 为准；本轮提出的默认行为和技术细节可在实施前调整，不视为已经由用户逐项确认。

## 1. 本轮目标

实现可独立于 Tauri 使用的 core、clipboard、network、storage 四个 Rust 库，通过局域网或已有 VPN 中可达的地址，在一台 Mac 和一台 Windows 电脑之间实现文字自动同步、手动发送和本地文字历史。

通过库接口、自动化测试和最小命令行示例完成验证。命令行示例放在 core 的 examples 中，只负责启动后端、接收测试操作和展示事件，不发展为独立 CLI 产品，也不承载业务规则。

本轮结束时，通过示例程序能够完成以下完整流程：

1. 启动两端后端，提供对端 IP 或主机名及端口，通过 core 返回的身份信息核对并调用接口完成配对。
2. 开启自动模式，在 Mac 复制文字，在 Windows 中正常粘贴；反方向也成立。
3. 切换手动模式，普通复制不发送，通过 core 的发送接口才发送。
4. 通过 core 查询和搜索本机历史，重新复制指定记录，重启后端后历史仍然存在。
5. 断开网络后收到离线事件；网络恢复后自动重连，并继续同步新的复制内容。

## 2. 范围与交付

| 部分 | 本轮交付 |
| --- | --- |
| 项目基础 | Cargo workspace、core / clipboard / network / storage 四个 Rust 库，以及 Windows / macOS GitHub Actions 构建与自动化测试；本轮不建立 Tauri 或 Web 前端工程 |
| 模块依赖 | core → clipboard / network / storage；底层库不反向依赖 core，四个库均不依赖 Tauri |
| 原生剪贴板 | macOS 轮询、Windows 事件监听；文字读写及非文字内容识别；自研 clipboard 实现不依赖 arboard |
| 设备连接 | 地址添加、首次身份确认、保存信任、在线与离线状态事件、自动重连、解除配对接口 |
| 文字同步 | 自动和手动发送、接收后写剪贴板、消息去重、自身写入识别、并发文字更新规则 |
| 文字历史 | 本机和远端文字的本地持久化、来源与时间、搜索、去重、重新复制、单条删除和清空 |
| core 公共接口 | 后端启动与关闭、配置、设备操作、模式切换、暂停与恢复、手动发送、历史操作、状态快照及事件订阅 |
| 工程交付 | 最小命令行示例、接口与使用说明、两端构建运行说明、关键业务测试、原生剪贴板测试、双设备联调记录 |

项目整体允许其他第三方库或插件间接引入 arboard。对自研 clipboard 的约束不能扩大为全局依赖禁令。

本轮拟建目录：

```text
crates/
├── core/
│   ├── src/
│   ├── tests/
│   └── examples/         # 仅用于调用 core 和双设备联调
├── clipboard/
├── network/
└── storage/
```

## 3. 建议的本轮默认规则

### 3.1 连接与配对

- 以一组 Mac ↔ Windows 为本轮验收范围，同一时刻只有一个活动对端；数据结构使用设备标识，便于以后扩展多设备。
- 先支持手动 IP 或主机名及端口，地址持久保存；局域网自动发现另行实现。
- 每台设备有稳定身份，地址变化不等于身份变化。
- 首次配对通过 core 返回本机与对端身份指纹，由示例程序显示并接收核对结果；双方调用确认接口后才开放剪贴板业务传输。
- 配对后使用加密且验证设备身份的连接。发现设备、知道地址或成功建立网络连接都不自动授予信任。
- 保存身份与配对信息；设备私钥使用系统安全存储，其具体实现随配对协议设计确定。
- 已知设备的身份变化时需要重新配对，不能静默替换信任。
- 解除配对后关闭该连接，不再接受该身份的业务消息，直至重新配对。
- 手动发送遇到离线或被暂停的接收端时返回明确结果，不把内容放入离线发送队列。

### 3.2 文字采集与同步

- macOS 以 250 ms 为初始轮询间隔，只在变化计数改变后读取必要内容；实际延迟与空闲资源占用需要测量。
- Windows 使用系统变化事件，避免以轮询作为正常采集路径。
- 文本保留 Unicode 内容，不去除首尾空白；跨平台换行表示按明确的转换规则处理。
- 本轮不发送或存储 HTML、RTF 等富文本表示，只处理其中可用的普通文字。若剪贴板实际包含图片或文件，则跳过它们的附带文本表示。
- 建议单条文字上限为 UTF-8 编码后 1 MiB；超过上限时跳过自动同步和历史入库，手动发送给出原因。
- 模式控制本机发送时机，允许接收单独控制；配对后建议默认手动发送、允许接收，自动模式由用户开启。
- 远端接收导致的本机写入不能再次广播；网络重复投递不能重复应用。
- 用户主动重新发送相同文字仍然有效，不能用永久内容去重阻止合法操作。
- 两端并发复制使用确定性的消息顺序，避免仅比较系统时间；在双方继续连接且无新复制时应得到一致结果。
- 消息顺序、身份绑定、重连会话与旧消息拒绝规则在网络实现开始前写入协议说明，并由测试覆盖。
- 启动和重连不自动发布当前已有剪贴板；恢复后处理新复制，已有内容可以手动发送。
- “暂停同步”停止业务内容收发与远端剪贴板写入，恢复时不重放暂停期间的旧消息。
- core 提供明确的启动和关闭流程；关闭时结束原生监听和网络任务，完成必要的存储收尾。任务生命周期由后端管理，不依赖界面是否存在。

### 3.3 历史

- 默认记录本机复制和已接受的远端文字，仅保存在本机；不进行完整历史数据库同步。
- “记录文字历史”是独立开关。切换手动发送或暂停同步不会自动关闭历史。
- 默认最多 1,000 条、保留 30 天，以先触发的限制为准；数量、保留时间和单条大小均纳入配置。
- 精确相同的文字合并为一条，更新最近时间与来源；保留记录的原始文本内容，不做 trim 等归一化。
- 从历史重新复制属于用户的新操作：自动模式可以发送，手动模式只更新本机剪贴板，等待用户主动发送。
- 单条删除与清空只影响本机历史，不修改系统剪贴板或对端历史。
- 本轮识别并跳过平台适配中明确支持的敏感或禁止历史标记；不承诺识别所有密码或密钥。
- 正文不写入运行日志。数据库的磁盘加密策略需要在历史持久化实现前明确，不将传输加密等同于本地数据库加密。

## 4. 本轮之外

以下仍属于后续产品方向，但不计入本轮完成条件：

- Tauri 桌面工程、React + TypeScript 前端、前端组件库和界面交互。
- 托盘、菜单栏、全局快捷键、桌面通知，以及窗口关闭后的常驻交互。
- 图片与文件传输、接收目录、传输进度及落盘后覆盖剪贴板。
- 文件夹、虚拟文件、大文件断点续传。
- mDNS 局域网自动发现。
- 多设备同时广播、设备分组及复杂权限矩阵。
- 完整历史同步、收藏、标签、OCR、富文本同步。
- 开机自启动、自动更新、应用商店发布，以及独立后台服务。

本轮需要识别图片和文件并跳过，避免误入文字历史；不要求先实现它们的读写和传输。平台 API 暴露出的后续兼容问题记录下来。

## 5. 实现顺序

| 阶段 | 交付点 | 验证方式 |
| --- | --- | --- |
| A. 工程与原生适配 | workspace、库接口边界，两端文字采集和写入 | 依赖方向检查；Mac 与 Windows 原生读写验证 |
| B. 本机文字历史 | SQLite 存储、入库规则、历史查询与操作接口 | 重启持久化、删除、去重、非文字排除测试 |
| C. 可信连接 | 协议说明、手动地址、配对、加密连接与重连 | 身份核对、未信任连接拒绝、重启重连和解除配对测试 |
| D. core 集成 | 模块组装与生命周期、自动与手动同步、暂停、回环与并发处理、状态与事件接口 | 核心业务测试和后端启动、关闭测试 |
| E. 双端联调与交付 | core 最小命令行示例、接口说明和两端构建运行说明 | 使用同一套 core 接口完成双设备端到端验证 |

业务测试使用必要的内存替身；系统剪贴板集成验证使用明确的测试文字，并记录环境。测试后端行为和状态边界，不为简单包装函数堆叠实现镜像测试。

## 6. 验收清单

- [ ] 四个库可在不安装前端依赖、不构建 Tauri 的情况下构建和测试；依赖方向符合约定。
- [ ] 在 Mac 和 Windows 上分别构建并运行 core 示例程序，记录操作系统版本、CPU 架构及构建命令。
- [ ] 能通过地址与 core 接口完成配对；未确认身份的设备不能注入或读取业务剪贴板内容。
- [ ] 两个方向分别验证中文、英文、emoji、多行代码、首尾空白及较长文字的复制粘贴。
- [ ] 自动模式下完成双向文字同步；手动模式下普通复制不发送，调用 core 发送接口有效。
- [ ] 在连接稳定的普通局域网中，以至少 1 秒的复制间隔，每个方向测试至少 20 次、每条不超过 8 KiB；目标为每次复制至对端可粘贴不超过 1 秒，记录测量结果。VPN 延迟单独记录并结合网络条件判断。
- [ ] 接收写入不回传，重复消息不重复应用；用户再次主动发送相同文字有效。
- [ ] 两端并发文字更新按协议确定性收敛；测试能够复现消息乱序与重复投递。
- [ ] 网络断开后状态快照和事件正确反映离线；恢复后自动重连，旧内容不覆盖当前剪贴板，新复制可以继续同步。
- [ ] 解除配对后停止业务通信；重新启动后端仍需遵守当前信任状态。
- [ ] 文字历史在重启后保留，搜索、重新复制、删除、清空和保留限制正常。
- [ ] 复制图片、文件以及其附带文本不会进入文字历史，也不会触发文字发送。
- [ ] 同步暂停与历史开关分别生效；显式关闭后端后原生监听与网络任务停止，必要数据已持久化。
- [ ] 示例程序仅调用 core 的公共接口，配对、同步与历史规则均保留在库内，未来接入 Tauri 无需重写这些规则。
- [ ] 关键测试通过，未通过的实机项目明确列出，不以编译成功代替跨平台行为验收。

## 7. 开工条件与交付边界

可立即开始工程骨架、原生文字适配和历史业务开发。配对及网络实现前，需完成消息协议与身份存储设计；这项设计属于本轮工作，不需要先实现图片与文件功能。

用户已有 Windows 电脑，开工时无需先配置远程访问。建立 Cargo workspace 时加入 GitHub Actions，使用 Windows 和 macOS 托管 runner 验证编译、Clippy、核心业务、存储及网络协议；网络集成测试可以在同一 runner 内启动多个端点。

原生剪贴板测试单独执行并记录会话环境与实际通过情况。托管 runner 中的测试不能代替用户桌面的剪贴板验收，两个独立 CI job 也不直接构成局域网中的双设备测试。文字流程打通后，在 Mac 和用户的 Windows 电脑上运行 core 示例，完成真实桌面与 LAN/VPN 联调；未完成实机项目时不能将本轮标记为已完成。

SSH 可在联调阶段按需配置，用于同步代码、构建及读取测试日志；不能假定 SSH 启动的程序与已登录用户共享桌面剪贴板。需要自动重复实机测试时，可在已登录 Windows 用户的终端中运行 self-hosted runner，仅执行可信工作流。相关依据见 [GitHub 托管 runner](https://docs.github.com/en/actions/concepts/runners/github-hosted-runners) 和 [Windows 窗口站](https://learn.microsoft.com/en-us/windows/win32/winstation/window-stations)。

目标系统最低版本、CPU 架构和 Rust 工具链在建立构建配置时记录。前端组件库、桌面打包和窗口生命周期留到后续桌面应用轮次确定，不作为本轮开工条件。

本轮候选实现沿用 Tokio、TCP + rustls、SQLite，以及两端必要的原生 API 绑定。具体版本在初始化项目时核对并锁定；Vite、Tauri 和前端依赖不在本轮引入。
